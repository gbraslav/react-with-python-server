# Temporary Assumptions

This document captures assumptions that apply to the current iteration only and should be revisited before production.

## Data

- The dataset is hard-coded on the server side with exactly 20 rows
- No database or external data source is used in this iteration
- The Flask API returns the same static 20 rows on every call
